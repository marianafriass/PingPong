#include "Ball.h"

Ball::Ball(int initPosition, int initVelocity, int initRadius)
    : position(initPosition), velocity(initVelocity), radius(initRadius) {}

void Ball::move() {
    // Lógica para mover la pelota
}

void Ball::bounce() {
    // Lógica para hacer que la pelota rebote
}

void Ball::reset() {
    // Lógica para reiniciar la pelota
}