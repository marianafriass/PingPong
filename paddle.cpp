#include "Paddle.h"

Paddle::Paddle(int initPosition, int initSize, int initSpeed)
    : position(initPosition), size(initSize), speed(initSpeed) {}

void Paddle::moveUp() {
    // Lógica para mover la paleta hacia arriba
}

void Paddle::moveDown() {
    // Lógica para mover la paleta hacia abajo
}

void Paddle::draw() {
    // Lógica para dibujar la paleta en la pantalla
}